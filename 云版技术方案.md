# 小小世界 · 双人云版技术方案（P2）

> 更新时间：2026-09-09 ｜ 状态：方案已确认，待开工 M1
> 本文件是 P2 双人云版的落地依据，配套：需求清单.md / PRD.md / 待完善功能清单.md

---

## 0. 已确认决策（2026-09-09）

| 项 | 决定 |
|---|---|
| 后端平台 | Supabase（免费额度：Auth + Postgres + Storage + Realtime） |
| 前端托管 | Vercel（免费 + HTTPS） |
| 注册邮箱 | 2732055594@qq.com（作为空间创建者 / 找回密码邮箱） |
| 域名 | 先用免费 xxx.vercel.app；国内访问体验不佳时再升级（Cloudflare 或国内服务器） |

### 免费域名够用吗
- 两人私密自用：够用，免费、HTTPS、无需备案。
- 注意：Vercel 在海外，国内访问速度一般、偶发不稳定。先用免费验证；不满意再优化，不影响后续迁移。

---

## 1. 目标架构（一句话）
复用现有手绘风界面 + Supabase 做「账号 / 数据库 / 图片存储 / 实时」+ Vercel 托管 = 真正的双人私密云版。

用户视角流程：
1. 你注册/登录（邮箱 2732055594@qq.com 或未来支持手机号）
2. 创建空间「小小世界」→ 系统生成邀请码（一次性，约 7 天有效）
3. 大哥注册后输入邀请码 → 绑定加入同一空间
4. 之后两人各自登录，看到并编辑同一份数据（点滴/愿望/悄悄话/纪念日）
5. 没有邀请码 / 不是空间成员的人：看不到、进不来（数据库行级权限兜底）

---

## 2. 数据模型（Supabase Postgres）

### auth（Supabase 内置，存账号）
- users：注册邮箱、密码哈希等，由 Supabase Auth 管理

### spaces（空间）
- id、name（小小世界）、owner_id、invite_code、invite_expires_at、created_at

### space_members（成员，上限 2）
- space_id、user_id、role(owner/member)、joined_at
- 唯一约束：一个 space 最多 2 名成员；一名用户只属于一个空间

### memories（点滴）
- id、space_id、created_by、record_date（记录日期，可补记）、text、mood、created_at、updated_at
- memory_photos：id、memory_id、storage_path（云存储路径）、sort
- comments：id、memory_id、user_id、text、created_at
- likes：memory_id + user_id（联合主键，天然「每人一次、最多 2」）

### wishes（愿望）
- id、space_id、created_by、text、cat、wish_date、status(open/done/gaveup)、done_date、gaveup_date、emoji 占位、created_at
- wish_photos：id、wish_id、storage_path、sort

### notes（悄悄话）
- id、space_id、author、text、at(日期+时分)、read_at（对方查看时间）、created_at
- note_replies：id、note_id、author、text、at

### anniversaries（纪念日）
- id、space_id、name、date、repeat、emoji、color、created_at

### 权限规则（RLS，安全核心）
- 所有业务表加行级安全：仅「本空间成员」可读可写
- 图片存储桶私有：仅空间成员经签名 URL 访问
- 邀请码：一次性使用；过期自动失效

---

## 3. 前端改造点
1. 新增页面：登录/注册、创建空间、输入邀请码、退出
2. 数据层替换：把现在 localStorage 读写改成调用 Supabase API（其余界面尽量复用）
3. 图片：上传到 Supabase Storage（自动压缩逻辑保留），数据库只存路径
4. 导入迁移：单机版导出的 .json 提供「导入云版」按钮，一键搬回忆
5. 同步：进入页面自动拉取 + 订阅 Realtime（对方新增/点赞/已读，本端实时出现）

---

## 4. 开发里程碑（每步可验收）

### M1 账号与空间绑定（✅ 已完成 2026-09-09）
- 邮箱验证已处理；账号经 Supabase 后台 Add user 添加
- 支持创建空间 / 邀请码加入（上限 2 人）/ 解散退出空间
- 报错已中文化（friendlyErr 映射）
- 注册/登录（邮箱 2732055594@qq.com）
- 创建空间 + 生成邀请码
- 另一半注册 + 输邀请码绑定（上限 2 人）
- 验收：两台设备两个账号，能进同一个空空间

### M2 数据上云
- 点滴/愿望/悄悄话/纪念日全部读写云端
- 单机 json 一键导入
- 验收：旧备份导入后内容完整、仍按新逻辑展示

### M3 图片上云（✅ 已完成）
- 照片存 Supabase Storage，仅空间成员可见
- 验收：手机传图 → 另一台设备可见

### M4 多端同步（✅ 已完成：整份数据实时推送）
- 点赞/评论/回复/已读实时同步
- 验收：A 手机写一条，B 手机约 1-2 秒内出现

### M5 上线打磨（✅ 已完成：Vercel 上线 + PWA + 缓存策略）
- Vercel 部署 HTTPS 正式地址（免费域名）
- PWA：手机「添加到主屏幕」像 App
- 安全复查：密码、RLS、邀请码、隐私

---

## 5. 需要你做的准备工作（到上线前逐步提供即可）
1. 注册 Supabase 账号（可用 2732055594@qq.com）→ 新建项目，拿到项目 URL + anon key
2. 注册 GitHub（存代码）与 Vercel（用 GitHub 账号登录即可自动部署）
3. 以上都做好后，把 Supabase 项目 URL/密钥填进配置，我就能带你联调上线

> 说明：我可以先把全部代码和数据库建表脚本写好（M1 起），等你注册好 Supabase/Vercel 后 10 分钟就能接上跑起来。邮箱 2732055594@qq.com 仅用于你注册平台账号与找回密码，网站内不会对外公开。

---

## 6. 风险与注意
- 免费额度：初期两人数据量完全够用；图片多了留意 Storage 用量（可在设置里看）
- 国内访问：Vercel 偶发慢/不稳定，如影响使用再优化
- 隐私：数据只对空间两人可见；密钥只放服务端环境变量，不进前端仓库

---

## 上线完成记录（2026-09-10）

- 生产地址：https://the-little-world-couple-diary.vercel.app/
- 入口：根地址自动进入云端版；未登录时回到 /cloud/ 登录页
- 账号：Supabase Auth（后台 Add user 创建），一人一个账号，绑定同一空间
- 数据：整份空间数据存 spaces.data（jsonb），照片存私有存储桶 couple-photos
- 同步：Supabase Realtime 推送，一方保存后另一方约 1-2 秒自动更新
- PWA：manifest + Service Worker（网络优先 v2）+ 自绘图标，可添加到手机主屏幕
- 踩坑记录：
  - Vercel 重新导入仓库后旧项目会失效（报 repository does not contain the requested branch or commit）
  - 访问 /cloud（无结尾斜杠）时相对路径会解析到根目录，已全部改为绝对路径
  - 旧 Service Worker 缓存会让普通窗口一直是旧代码（无痕正常），已改为网络优先并升级缓存版本
  - Vercel 不会把 vercel.json 当静态文件提供，访问 /vercel.json 返回 404 属正常
- 国内访问：vercel.app 在国内不稳定/可能被拦，如日常使用受影响，可迁到香港或国内服务器（前端纯静态，传文件 + 改 Supabase URL Configuration 即可）