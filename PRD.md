# 情侣生活记录网站 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品代号 | 情侣记录网站（暂定，可再想个好名字） |
| 版本 | v0.1（第一轮需求讨论后） |
| 日期 | 2026-09-07 |
| 状态 | 需求细化中，尚未进入开发 |

## 1. 产品定位

> **两个人的私密数字回忆本**：把「在一起的日子、共同经历的点滴、想一起做的事、想对对方说的话」沉淀成一个只属于两个人的、会随时间自然生长的空间。

- 核心对象是「我们」，不是「我」：内容由双方共同创建、共同拥有、双向可见。
- 不是公开社交、不是单向日记：默认**完全私密**，只有绑定在一起的两个人能进入。
- 情感基调：温暖、轻松、有手工感；视觉采用**手绘涂鸦风（Hand-Drawn Doodle）**。

## 2. 目标用户与核心场景

**用户画像**：处于恋爱/婚姻关系中的情侣，希望记录两个人共同的生活点滴，重视回忆留存与仪式感；以手机随手记录为主，偶尔在电脑上回顾整理。

**核心场景**：
1. 打开首页看到「我们在一起已经 X 天」，下一个纪念日还有几天；
2. 今天一起做了顿饭 / 去了某个地方，随手发一条带照片的点滴；
3. 定下一个「想和你一起做的事」，某天实现了就划掉，留下完成日期；
4. 想说点不好意思当面说的话，写在悄悄话里，对方一打开就能看到；
5. 偶尔从头翻翻时光轴，重温一路走来的过程。

## 3. 已确认的关键决策（本轮讨论结论）

| # | 讨论项 | 结论 |
|---|---|---|
| 1 | 产品形态路线 | 先做**单机可跑原型**验证体验与视觉，验证后再升级为**双人账号版** |
| 2 | 私密性 | **完全私密**，仅绑定在一起的两人可进入（密码 / 邀请码） |
| 3 | 数据持久化 | **稳妥优先**：最终采用服务端数据库 + 多端同步；原型阶段本地存储 + 一键备份导出，避免丢数据 |
| 4 | 悄悄话 / 留言 | **每条必须带日期**（自动记录发送时间并在卡片上显眼展示） |
| 5 | 愿望清单 | **许愿要有日期，完成也要有日期**（创建时记录许愿日期；勾选完成时记录完成日期，两者都展示） |
| 6 | 视觉风格 | **手绘涂鸦风**（规范见 §7，全站严格执行，禁止风格漂移） |

## 4. 功能模块细化（MVP = 第一版单机原型）

### 4.1 首页 · 我们的天数
- 大数字展示「在一起 X 天」（今天 − 设置里的在一起日期）；
- 纪念日卡片：内置「在一起纪念日」并支持自定义（100 天、一周年、生日……），显示「还有 X 天」；
- 「记一条今天的小确幸」快速入口，一键跳到时光轴发今天的点滴。

### 4.2 点滴时光轴（核心模块）
- 记录字段：正文文字、照片（可多张）、心情标签、记录日期（默认今天，可改用于补记）；
- 按时间倒序，支持按「年月」分组浏览；
- 每条可点赞、评论；
- 预留「谁记录的」字段（双人版需要，原型阶段先存）。

### 4.3 愿望清单
- 每条愿望：内容 + **许愿日期**（创建时自动记录，可修改）+ 状态（待实现 / 已完成）；
- 勾选完成时自动写入**完成日期**，卡片同时展示「许愿于 X」「完成于 X」，可附完成照片；
- 支持简单分类（一起旅行 / 一起吃 / 一起体验 / 其他）。

### 4.4 悄悄话（私密留言板）
- 给对方写一张便签：内容 + **日期**（自动记录，卡片上显眼展示，如「2026-09-07 留」）；
- 按时间排列，支持回复形成小对话（待细化，见功能清单 #6）；
- 对方查看后可标记「已读」。

### 4.5 设置
- 两人的昵称 / 符号、在一起日期、纪念日管理、数据备份导出 / 恢复导入、清空数据；
- 账号绑定、通知开关等放 V2。

## 5. 双人协作与权限模型（V2 双人账号版）
- 注册 / 登录 + 邀请码绑定：A 创建空间生成邀请码，B 输入后加入，一个空间默认上限 2 人；
- 双方默认对全部内容可读写（共同拥有）；每条内容记录「谁创建」；
- 删除类操作策略待细化（是否需要双方确认 / 回收站）；
- 解绑、一方退出后数据归属需明确（见功能清单）。

## 6. 技术方案与演进路线

| 阶段 | 形态 | 技术 | 数据 |
|---|---|---|---|
| Phase A（先做） | 单机原型，验证体验与视觉 | 纯前端 HTML/CSS/JS（手绘涂鸦风），无需安装、打开即用 | 浏览器本地存储 + **导出 / 导入备份文件**；数据层做抽象，方便迁移 |
| Phase B（可选） | 数据稳妥化 | 本地轻量服务 Node + SQLite（或 PWA） | 数据落盘，支持从原型一键迁移 |
| Phase C | 双人账号云版 | 前后端分离 + 云数据库 + HTTPS | 账号、邀请绑定、多端同步、权限与隐私 |

> 数据层从设计第一天就抽象成统一接口（load / save / add / remove…），确保 Phase A 的本地数据能平滑迁移到 Phase B / C。

### 数据模型草案（关键字段）
- couple：id、在一起日期、昵称A / 昵称B、创建时间
- memory（点滴）：id、记录日期、文字、照片[]、心情、创建人、点赞、评论[]
- wish（愿望）：id、内容、**许愿日期**、**完成日期**（可空）、完成照片[]、分类、状态
- note（悄悄话）：id、内容、**日期**、已读、回复到（可空）
- anniversary（纪念日）：id、名称、类型、日期、是否每年重复

## 7. 视觉设计规范（手绘涂鸦风）— 全站强制，禁止漂移
- 奶白纸张背景 #fffef5 + 笔记本横线（repeating-linear-gradient）+ 红色页边线；
- 主色墨黑 #2c2c2c；标记笔三色：红 #ff6b6b、蓝绿 #4ecdc4、黄 #ffd93d；
- 虚线边框 border-dashed、小圆角 rounded-sm、硬边偏移阴影 shadow-[Npx_Npx_0px_颜色]；
- 元素带轻微旋转模拟手写不规则感；装饰：胶带 / 图钉 / 回形针 / 咖啡渍 / 手绘星星与波浪线（SVG）；
- 交互：hover 手绘颤动（微小位移 / 旋转）、active 按压压平（scale 缩小 + 阴影消失）、focus 清晰可见；提供 prefers-reduced-motion 方案；
- **禁止**：渐变（bg-gradient-*）、大圆角（rounded-lg 及以上 / rounded-none / rounded-full）、实线边框 border-solid、精确阴影（shadow-md / lg / xl）、等宽字体 font-mono、backdrop-blur、玻璃态；
- 自检：按钮 / 卡片 / 输入框严格按规范 token 书写；正文对比度达 WCAG AA；移动 / 平板 / 桌面无横向溢出。

## 8. 里程碑
- M1：PRD 与功能清单定稿（本文档 +《待完善功能清单.md》）— 进行中
- M2：Phase A 单机原型（核心四模块 + 手绘涂鸦风 + 数据导入导出 + 预填示例数据）
- M3：体验走查 → 按反馈修订原型 → 冻结 MVP 视觉与交互
- M4：Phase C 双人账号版（账号 / 绑定 / 云数据库 / 多端同步）

## 9. 开放问题
不阻塞开发，边做边定；影响首页与视觉的（两人称呼、在一起日期、示例昵称）先在原型里用默认值，你们随时可改。完整清单见《待完善功能清单.md》。
