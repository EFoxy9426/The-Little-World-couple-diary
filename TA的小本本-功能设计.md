# TA 的小本本 · 功能设计（v0.2）

> 定位：我私下记下的、关于 TA 的细节。默认只有我自己能看；TA 可以申请查看，是否同意由我决定。
> 状态：设计稿，一期已确认范围，数据库脚本已就绪（cloud/schema-partner-notes.sql）。

## 0. 已确认决策（2026-09-11）

1. **存在可见性默认关闭**：`requestable` 的记录默认连「有记录存在」都不显示给被记录者，由记录者手动开启 `existence_visible`。
2. **一期包含「按分类申请」**：申请范围支持 `all`（整本）/ `category`（按分类）/ `note`（单条）。
3. **一期包含删除请求**：被记录者可发起 `kind = deletion` 的请求，由记录者批准（软删除）或拒绝。
4. **可见性简化为两种**（2026-09-11）：新建/编辑只提供「可申请查看」与「直接给 TA 看」；旧数据若为 `private`，编辑时保留「仅自己可见（旧设置）」选项，不强改。默认值为「可申请查看」（配合存在可见性默认关闭，仍保持私密）。
5. **界面简化（A+C 结合）**：3 个页签（我的记录 / 关于我的 / 申请与授权）；卡片折叠、正文 2 行截断、点开看详情；新建/编辑为底部弹层，高级项收进「更多选项」；状态色收敛为黄（可申请）/ 绿（已共享）。

---

## 1. 设计原则

1. **默认最私密**：新建即「仅自己可见」，共享必须显式操作。
2. **拒绝是权利**：不同意不需要理由，也不影响任何关系数据。
3. **可撤销、可过期**：授权不是一次给就永远给。
4. **不做「给 TA 打分」的工具**：产品文案引导「更懂 TA」，而不是「审查 TA」。
5. **只补充不改写**：被记录者可以添加说明，但不能修改记录者的原文。

---

## 2. 典型场景

- A 发现 B 不喜欢吃鱼 → 记下来，以后点菜避开。
- B 想知道 A 记了什么 → 点「申请查看」→ A 批准其中 3 条、有效期 7 天。
- A 记了一条「B 不喜欢听我说"随便"」这类敏感内容 → 默认不开放申请。
- B 看到「不喜欢吃鱼」这条 → 添加说明「其实我只是不喜欢河鱼」→ A 收到补充信息。

---

## 3. 入口与信息架构

不改动底部导航（手机上已有 5 个 tab），放进现有「💌 悄悄话」内做子页签：

    悄悄话
    ├── 写给 TA（现有悄悄话）
    └── TA 的小本本（新增）
        ├── 我的记录（我写的，按分类分组）
        ├── 收到的申请（我是被记录方：发起 / 查看申请状态）
        └── 授权管理（查看 / 撤销我给出的授权）

分类（固定 5 类）：口味忌口 / 爱好想要 / 雷区 / 习惯作息 / 其他。

---

## 4. 数据模型（Supabase）

### partner_notes（记录本体）

| 字段 | 说明 |
|---|---|
| id | uuid 主键 |
| space_id | 所属空间 |
| author_id | 记录者 |
| about_user_id | 被记录者 |
| category | 分类 |
| text | 正文 |
| visibility | private / requestable / shared |
| created_at / updated_at | 时间戳 |

### note_shares（授权）

| 字段 | 说明 |
|---|---|
| note_id | 对应记录 |
| viewer_id | 被授权的人 |
| granted_by | 批准人 |
| granted_at | 批准时间 |
| expires_at | 过期时间（可空 = 永久） |
| revoked_at | 撤销时间（非空 = 已失效） |

### note_access_requests（申请）

| 字段 | 说明 |
|---|---|
| id / space_id | 主键 / 空间 |
| requester_id | 想查看的人 |
| owner_id | 记录者 |
| scope | all / category / note |
| scope_value | 分类名或 note_id |
| message | 申请留言（可选） |
| status | pending / approved / denied / expired |
| created_at / responded_at | 时间戳 |

### note_view_logs（查看日志）

note_id、viewer_id、viewed_at —— 让记录者知道谁在什么时候看过。

---

## 5. 可见性状态机

| 状态 | 记录者 | 被记录者 |
|---|---|---|
| private | 可读写 | 完全不可见 |
| requestable | 可读写 | 只能看到「有记录存在」与分类，看不到内容，可申请 |
| shared | 可读写 | 可直接阅读 |

读权限判定：visibility = shared，或存在有效 note_shares（未撤销、未过期）。

---

## 6. 核心流程

### A. 记录
分类 → 正文（可选「为什么记」）→ 可见性三选一（默认「仅自己」）→ 保存。

### B. 申请
被记录方看到提示卡：「TA 记录了一些关于你的细节，你可以申请查看。」
→ 选择「申请整本」或「按分类申请」→ 可写一句说明 → 提交。

### C. 审批（记录者）
打开申请 → 逐条勾选 + 有效期（7 天 / 30 天 / 永久）+ 只读或可评论 → 批准 / 拒绝。

### D. 查看（被记录方）
列表顶部固定提示：**以下为 TA 的观察，可能不准确。**
每条可「添加说明」，说明进入记录者的「补充信息」区，不改原文。

### E. 撤销
记录者在「授权管理」一键撤销 → 立即失效；被记录方看到「内容不再可见」。

### F. 过期
到期自动失效（定时任务或读取时判定），状态标记 expired，可一键续期。

---

## 7. 页面清单

| 页面 | 关键元素 |
|---|---|
| 我的记录 | 分类筛选、条目卡（含可见性标签）、搜索、新建 |
| 记录详情 / 编辑 | 正文、可见性切换、删除、授权入口 |
| 收到的申请 | 申请状态、撤销申请、查看已授权内容 |
| 审批页 | 逐条勾选、有效期、同意 / 拒绝 |
| 授权管理 | 谁可看、何时过期、撤销、续期 |
| 提示卡 | 首页 / 悄悄话页的轻提示，可关闭 |

---

## 8. 权限（RLS）要点

- partner_notes：记录者 all；被记录者仅 shared 或有效授权可 select。
- note_shares：仅双方可读写；撤销写 revoked_at。
- note_access_requests：仅 requester 与 owner 可见。
- note_view_logs：仅记录者可读。
- **前端隐藏不算安全**，必须由数据库行级权限兜底。

RPC 建议：
- request_note_access(scope, value, message)
- respond_note_request(id, approve, note_ids[], expires_in)
- revoke_note_share(note_id, viewer_id)
- my_partner_notes()（返回记录 + 授权状态 + 是否可读）

---

## 9. 通知

- 记录者：收到申请（红点 + 提示卡）
- 被记录者：申请通过 / 被拒 / 被撤销
- 一期只做站内提示；二期再接邮件或 PWA 通知。

---

## 10. 分期计划

### 一期（MVP）
- 分类 + 正文 + 三档可见性
- 整本申请 → 批准 / 拒绝
- 授权可撤销、可设 7 天有效期、只读

### 二期
- 逐条勾选授权、按分类申请
- 补充说明 / 申请更正
- 查看日志、红点通知

### 三期
- 关照提醒（忌口、生日）与首页打通
- 导出、复盘、模板化条目

---

## 11. 文案规范

| 场景 | 推荐 | 避免 |
|---|---|---|
| 新建 | 记一条关于 TA 的小细节 | 添加对方档案 |
| 可见性 | 默认只有我看得到 | 设置权限 |
| 申请 | 想了解 TA 记下的事 | 申请查看 TA 的记录 |
| 被拒 | TA 暂时没有同意查看 | TA 拒绝了你 |
| 查看页顶部 | 这是 TA 的观察，可能不准确 | （无提示） |

---

## 12. 上线前检查清单

- [ ] 新建条目默认 private（UI 与数据库双重保证）
- [ ] 未授权用户接口返回空，不能只靠前端隐藏
- [ ] 撤销 / 过期后立即失效（含已打开的页面）
- [ ] 拒绝不暴露原因；申请仅相关两人可见
- [ ] 敏感内容提示（健康 / 家庭 / 宗教等）已就位
- [ ] 导出备份包含这部分数据并标注为私密

---

## 13. 边界提醒

- 不鼓励记录健康、家庭、宗教、性取向等敏感信息；如记录需显式提示「请确保已征得对方同意」。
- 拒绝授权不应影响任何其他功能的使用。
- 产品角度：这是「更懂 TA 的工具」，不是「监控 / 背调工具」。